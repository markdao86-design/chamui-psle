# 锤锤的备考冒险 - 完整开发说明文档

> **项目代号**: chamui-psle
> **当前版本**: v0.1 (本地 MVP)
> **目标用户**: 新加坡 P5 学生(DP 身份),备考 PSLE 2027
> **创建日期**: 2026-04-30
> **文档用途**: 供 Claude Code 等开发工具读取,继续开发更复杂功能

---

## 第一部分:项目背景

### 1.1 用户画像

- **学生**: 5 年级在读,DP(Dependent Pass)身份
- **PSLE 时间**: 2027 年 9 月(73 周备考期)
- **当前成绩** (P5):
  - 英语 65 分(AL5/6,最大短板)
  - 科学 85 分(AL2,中等偏上)
  - 数学 90+ 分(AL1)
  - 华文 90+ 分(AL1)
- **英语失分点**: 阅读 + 完形 + 作文 + 口语(整体语感不足)
- **抗压能力**: 一般,需要劳逸结合
- **特长 / DSA**: 无,纯靠笔试
- **PSLE 目标**: 总分 9-12 分(DP 良好中档校,如立化/英华/淡马锡)
- **作息要求**:
  - 平日 21:30 准时睡觉
  - 周日下午 14:00-18:00 完全休息(雷打不动)
  - 周六 19:30 后 2h 自由家庭活动

### 1.2 备考手册总览(配套文档)

学生使用的备考手册有两份:
- **PSLE_2027_完整备考总手册_v9.pdf**(79 页)— 包含 73 周完整周卡片
- **PSLE_2027_积分激励系统.pdf**(2 页 A4)— 积分规则单页

**手册核心结构**:
```
四阶段 73 周计划:
├── 第一阶段 W1-W26 (2026.5-10) "基础补漏"
│   ├── W1-W14: P3-P4 系统过 + 难易分级
│   └── W15-W26: P5 收尾
├── 第二阶段 W27-W52 (2026.11-2027.4) "系统提升"
│   ├── W27-W42: P6 系统学
│   └── W43-W52: 真题前移
├── 第三阶段 W53-W65 (2027.5-7) "刷题冲刺"
└── 第四阶段 W66-W74 (2027.8-9) "考前调整"
```

**每周时间分配 30.25 小时**:
- 英语 13.75h (45%) - 最大投入,因为最弱
- 科学 8.25h
- 数学 3.5h
- 华文 2.75h
- 复盘 2h

**每日时段模板**:
```
平日:
  16:30-18:00 段 1 学习 (1.5h)
  19:00-20:30 段 2 学习 (1.5h)
  20:30-21:00 段 3 学习 (0.5h)
  21:30 睡觉

周六: 7.5h 学习 (上午科学 + 下午英语到 19:30)
周日: 6h 学习 (上午科学华文 + 下午休息 + 晚上数学)
```

---

## 第二部分:积分激励系统(核心业务规则)

### 2.1 积分规则(必须严格遵守)

#### 加分项

| 类型 | 触发条件 | 积分 | 频率 |
|------|---------|------|------|
| 基础 | 每周日完成复盘 | +5 | 周 |
| 基础 | 月小测达标(W4 / W8 / W12) | +20 | 月 |
| 里程碑 | W14 中期模拟卷 (P3-P4 综合) | +50 | 单次 |
| 里程碑 | W20 P5 综合复习 | +30 | 单次 |
| **终极** | **W26 第一阶段总模考** | **+100** | **单次** |
| 额外 | 英语作文单篇 30+ 分 | +5 | 单次 |
| 额外 | 老师特别表扬(任一老师) | +10 | 单次 |
| 额外 | 连续 4 周无老师反馈学习问题 | +15 | 滚动 |

#### 扣分项

| 触发条件 | 扣分 |
|---------|------|
| 老师反馈学习问题(每次) | -10 |

**扣分细节**:
- 同周同老师只扣一次(避免轰炸)
- 不同老师独立计算
- 积分可扣到负数,但负数期间不能兑换
- 触发后角色 24 小时显示沮丧表情

#### "学习问题"定义(避免后期模糊)

✅ **算**:不交作业 / 课堂走神 / 错题不订正 / 作业敷衍 / 不专心听讲
❌ **不算**:某次发挥失常、某题不会(这是能力,不是态度)

### 2.2 兑换规则

- **汇率**: 1 分 = SGD 0.5
- **兑换方式**: 父母带孩子去现场买
- **❌ 不能买**: 游戏类(Switch 游戏卡 / Roblox 充值 / PS5 / 手游充值 / 电脑游戏 / 相关配件)
- **✅ 可以买**: 机器人、LEGO、玩具、文具、书、零食、衣服 — 其他都行
- **频率**: 每月可花一次,也可以攒着不花
- **有效期**: 无限期累积(可以攒到 PSLE 后)

### 2.3 第一阶段 26 周积分预估

| 时间 | 累积积分(正常发挥) | 兑换价值 SGD |
|------|---------------------|------------|
| W4 月评估 | ~45 | 22.5 |
| W8 | ~95 | 47.5 |
| W14 中期里程碑 | ~165 | 82.5 |
| W26 终极 | ~280 | 140 |

**17 个月全程预估**: ~740 分 = SGD 370

---

## 第三部分:技术架构

### 3.1 当前实现(v0.1)

**技术栈**:
- 纯前端: HTML + CSS + JavaScript(无框架)
- 数据存储: localStorage(浏览器本地)
- 单文件部署: 一个 `index.html`(63 KB)

**为什么选这个栈**:
- 用户需求是"GitHub Pages 部署"和"Firebase 云同步"
- 纯前端 = 无需服务器
- 单文件 = 部署/分享/备份都简单
- 无框架 = 任何浏览器都能跑

**文件结构**(开发时拆开,部署时合并):
```
chamui-psle/
├── index.html              # UI 骨架 + CSS
├── character.js            # 锤锤 SVG 渲染器
├── data.js                 # 数据存储 + 规则引擎
├── app.js                  # 主应用逻辑
└── chamui_app_single.html  # 合并后的单文件部署版
```

### 3.2 模块说明

#### `character.js` - CHAMUI 命名空间

```javascript
const CHAMUI = {
  levels: [...]      // 8 级配置
  equipment: [...]   // 10 件装备配置
  
  getLevelInfo(points)        // 根据积分得当前等级
  getNextLevelInfo(points)    // 获取下一级
  checkEquipmentUnlocked(equipId, state)  // 装备解锁检查
  renderCharacter(state)      // 生成 SVG 字符串(核心)
}
```

**SVG 渲染策略**:
- viewBox 220x240
- 分层绘制: 火焰背景 → 披风 → 书包 → 身体 → 头饰 → 手持武器 → 配饰
- 每层根据装备状态条件渲染
- 用 SVG 内嵌 `<animate>` 实现持续动画(免 JS)

#### `data.js` - 数据层

```javascript
// 26 周静态数据
const WEEK_DATES = [...]    // ['5.4-5.10', ...]
const WEEK_THEMES = [...]   // 每周主题
const KEY_WEEKS = [4,8,12,14,20,26]  // 关键里程碑

// 状态结构
function getDefaultState() {
  return {
    version: 1,
    currentWeek: 1,
    totalPoints: 0,
    weekly: {},          // { 1: { checkin: { review: true, ... } } }
    streakBonusCount: 0,
    monthlyTestPass: 0,
    milestones: { W14: false, W20: false, W26: false },
    sadUntil: null,      // 沮丧表情过期时间戳
    logs: [],            // 加分日志
    exchanges: []        // 兑换记录
  }
}

// 周打卡模板生成器(根据周数返回应该有的项目)
function getWeeklyCheckinTemplate(weekNum) {
  // 返回数组 [{ id, label, desc, points, type, adminOnly }, ...]
}

// 持久化
function loadState()      // 从 localStorage 加载
function saveState(state) // 保存到 localStorage
function recalcTotalPoints(state)  // 重算总积分
```

#### `app.js` - 应用逻辑

**主要函数**:
```javascript
init()                    // 初始化
renderAll()               // 重渲染所有 UI

// 各页面渲染
renderHeader()
renderDashboard()         // 主页(角色+进度+装备)
renderCheckinPage()       // 打卡页
renderHistoryPage()       // 历史(表格+曲线图)
renderAdminPage()         // 管理(加分日志)

// 打卡交互
toggleCheckin(week, itemId)  // 切换某项打卡状态

// 管理操作
addPoints(reason, points)    // 手动加分/扣分
deleteLog(idx)               // 删除日志

// 兑换
addExchange()                // 记录新兑换
deleteExchange(idx)          // 删除兑换

// 升级动画
checkLevelUp(oldPoints, newPoints)
showLevelUpAnimation(newLevel)

// 工具
exportData()                 // 导出 JSON
importData(event)            // 导入 JSON
manualWeekChange()           // 手动切换当前周
resetData()                  // 重置
showToast(msg, type)         // 通知

// 数据可视化
drawChart()                  // 用 Canvas 画积分曲线
```

### 3.3 数据流

```
用户操作(点击打卡/加分按钮)
    ↓
更新 state 对象(JS 内存)
    ↓
saveState(state)  →  localStorage 写入
    ↓
recalcTotalPoints(state)  →  重算积分
    ↓
checkLevelUp()  →  如果跨级,触发升级动画
    ↓
renderAll()  →  重渲染所有 UI(主页/打卡/历史/管理)
```

---

## 第四部分:UI/UX 设计

### 4.1 设计风格

**风格定位**: Q 萌可爱 + 卡通边框 + 高饱和色彩

**核心色板**:
```css
--color-primary: #FF6B6B;    /* 锤锤红 */
--color-secondary: #4ECDC4;  /* 薄荷绿 */
--color-accent: #FFE66D;     /* 阳光黄 */
--color-purple: #A788E0;     /* 魔法紫 */
--color-pink: #FFB6D9;       /* 樱花粉 */
--color-bg: #FFF8E7;         /* 米色背景 */
--color-text: #2D3047;       /* 深灰主文 */
```

**字体**:
- 显示字体: `ZCOOL KuaiLe`(中文活泼) + `Fredoka`(英文圆润)
- 正文: `Noto Sans SC`

**视觉特点**:
- 4px 黑色描边(卡通风格)
- 3-4px 黑色阴影偏移(立体感)
- 圆角 12-24px(柔和)
- 各种 keyframe 动画(漂浮、发光、弹跳)

### 4.2 页面结构

```
/ (单页应用)
├── Header: Logo + 积分/等级/周次胶囊
├── Tabs: [主页] [打卡] [历史] [管理]
└── Pages:
    ├── #page-dashboard
    │   ├── .character-card (锤锤 SVG + 称号 + Lv)
    │   ├── .progress-section (进度条 + 3 个统计)
    │   └── .equipment-grid (10 个装备图标)
    ├── #page-checkin
    │   ├── .checkin-week-header (上周/下周导航)
    │   └── .checkin-list (本周任务清单)
    ├── #page-history
    │   ├── 26 周积分曲线图(Canvas)
    │   ├── 26 周追踪表
    │   └── 兑换记录
    └── #page-admin
        ├── 加分操作面板(8 类按钮)
        ├── 系统工具(导出/导入/重置)
        └── 加分日志
```

### 4.3 锤锤角色设计

**8 级形态命名**:
1. 🐣 萌新锤锤 (0-29)
2. 📚 学徒锤锤 (30-69)
3. ✏️ 笔尖锤锤 (70-119)
4. 🎒 书童锤锤 (120-179)
5. 🔬 研究锤锤 (180-249)
6. 🦸 战士锤锤 (250-329)
7. 🌟 学霸锤锤 (330-419)
8. 👑 王者锤锤 (420+)

**10 件装备解锁**:
| 装备 | 触发条件 |
|------|---------|
| 🎓 学士帽 | 累积 30 分 |
| ✏️ 铅笔剑 | 累积 70 分 |
| 🎒 书包 | 累积 120 分 |
| 🔬 试管 | 完成 W14 |
| 🛡️ 盾牌 | 4 周无问题 |
| 🦸 披风 | 月小测连续 3 次 |
| 👓 眼镜 | 累积 200 分 |
| 🏆 奖杯 | 完成 W20 |
| 👑 皇冠 | 完成 W26 |
| 🔥 火焰特效 | 任一里程碑 |

**SVG 设计要点**(完全原创,无版权问题):
- 圆头圆身体(像水滴/雪人)
- 大眼睛(开心/沮丧两种状态)
- 红脸蛋
- 装备可以同时穿戴(分层叠加)
- 持续动画: idle-bounce(上下漂浮)
- 沮丧时眉毛下垂、嘴角下弯

---

## 第五部分:V0.1 已实现功能清单

### ✅ 已完成

- [x] 4 个标签页 UI(主页/打卡/历史/管理)
- [x] 锤锤 SVG 角色(8 级 × 10 装备 = 复杂渲染)
- [x] 积分自动计算(打卡 + 手动加分 + 扣分)
- [x] 8 级升级系统 + 升级弹窗动画
- [x] 装备解锁系统(支持 4 种条件: points / milestone / streak / monthly3)
- [x] 26 周打卡清单(动态根据周数生成,关键周有特殊项)
- [x] 26 周追踪表
- [x] 积分曲线图(Canvas 手绘,关键周高亮)
- [x] 兑换记录
- [x] 加分日志(可删除)
- [x] 数据导入/导出(JSON)
- [x] 周次切换(上一周/下一周/手动跳转)
- [x] Toast 通知
- [x] 沮丧表情系统(老师反馈学习问题后 24h)
- [x] localStorage 持久化
- [x] 响应式布局(移动端可用)

### ❌ 未实现(下个阶段)

- [ ] **Firebase 云同步**(最关键!需要父母和孩子各自设备同步)
- [ ] **用户认证**(区分家长/孩子角色)
- [ ] **角色权限**(孩子不能用管理页加分)
- [ ] **PWA 支持**(可装成桌面/手机应用)
- [ ] **推送通知**(每周日提醒打卡)
- [ ] **第二/三/四阶段周卡片**(目前只有 W1-W26)
- [ ] **更细粒度的打卡项**(目前每周只有 1-3 项,可以做成每天打卡)
- [ ] **作文老师反馈集成**(上传作文图片 + AI 评分?)
- [ ] **模考成绩录入**(分科目记录,不只是"达标/不达标")
- [ ] **趋势分析**(比如"英语连续 3 周下降")
- [ ] **多语言**(英文版给孩子用?)
- [ ] **音效**(升级、加分的反馈音)
- [ ] **更多装备/外观**(目前只有 10 件,可扩展到 20+)
- [ ] **成就系统**(独立于装备的徽章系统)
- [ ] **每日任务**(不只是周任务,接入备考手册的每日时段模板)

---

## 第六部分:扩展开发指南

### 6.1 加新装备

在 `character.js` 的 `CHAMUI.equipment` 数组添加:

```javascript
{
  id: 'newEquip',           // 唯一 ID
  icon: '🎵',                // emoji 图标(用于装备网格显示)
  name: '音乐盒',            // 显示名
  condition: 'points',      // 触发条件类型: points / milestone / streak / monthly3
  value: 100,               // 触发值
  hint: '累积 100 分'        // 未解锁时显示的提示
}
```

然后在 `renderCharacter(state)` 函数里加 SVG 绘制逻辑:

```javascript
// 在 renderCharacter 函数内
const has = {};
this.equipment.forEach(eq => {
  has[eq.id] = this.checkEquipmentUnlocked(eq.id, state);
});

// 绘制新装备
const newEquip = has.newEquip ? `
  <g transform="translate(170, 100)">
    <!-- SVG 内容 -->
  </g>
` : '';

// 在最终 return 的 SVG 里插入 ${newEquip}
```

### 6.2 加新等级

在 `character.js` 的 `CHAMUI.levels` 添加:

```javascript
{ lv: 9, name: '传说锤锤', title: '突破极限', minPoints: 600, color: '#FFE4ED' }
```

注意: `getNextLevelInfo` 使用 `levels[current.lv]` 索引,所以新等级要按顺序加。

### 6.3 加新打卡项

修改 `data.js` 的 `getWeeklyCheckinTemplate(weekNum)`:

```javascript
function getWeeklyCheckinTemplate(weekNum) {
  const items = [
    { id: 'review', label: '✅ 周日完成复盘', desc: '...', points: 5, type: 'core' }
  ];
  
  // 加新项 - 比如每周英语作文
  items.push({
    id: 'essay',
    label: '✏️ 完成本周英语作文',
    desc: '老师批改并重写',
    points: 3,
    type: 'core'
  });
  
  // ... 现有的特殊周逻辑
  return items;
}
```

### 6.4 升级到 Firebase 云同步

参考 `锤锤备考_部署指南.md`,核心是替换 `loadState` / `saveState`:

```javascript
// 替换 data.js 中的两个函数

async function loadState() {
  return new Promise(resolve => {
    waitForFirebase(async () => {
      firebase.initializeApp(firebaseConfig);
      const db = firebase.firestore();
      const doc = await db.collection('chamui').doc('main').get();
      if (doc.exists) {
        resolve(Object.assign(getDefaultState(), doc.data()));
      } else {
        resolve(getDefaultState());
      }
    });
  });
}

async function saveState(state) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(state));  // 本地备份
  if (typeof firebase !== 'undefined') {
    await firebase.firestore().collection('chamui').doc('main').set(state);
  }
}
```

**注意**: `loadState` 改为 async 后,`init()` 也要改:

```javascript
async function init() {
  state = await loadState();
  recalcTotalPoints(state);
  bindEvents();
  renderAll();
}
```

### 6.5 加 PWA 支持

需要 3 件事:

1. **manifest.json**:
```json
{
  "name": "锤锤的备考冒险",
  "short_name": "锤锤",
  "icons": [{ "src": "icon-192.png", "sizes": "192x192" }],
  "start_url": "/",
  "display": "standalone",
  "background_color": "#FFF8E7",
  "theme_color": "#FF6B6B"
}
```

2. **Service Worker** (sw.js):
```javascript
self.addEventListener('install', e => {
  e.waitUntil(caches.open('chamui-v1').then(c => c.addAll(['/', '/index.html'])));
});
self.addEventListener('fetch', e => {
  e.respondWith(caches.match(e.request).then(r => r || fetch(e.request)));
});
```

3. **HTML 注册**:
```html
<link rel="manifest" href="/manifest.json">
<script>navigator.serviceWorker?.register('/sw.js');</script>
```

### 6.6 加用户认证(Firebase Auth)

```javascript
// 启用后,管理页只对家长可见
async function checkUserRole() {
  const user = firebase.auth().currentUser;
  if (!user) return 'guest';
  // 家长邮箱白名单
  const parentEmails = ['parent@example.com'];
  return parentEmails.includes(user.email) ? 'parent' : 'child';
}

// UI 控制
const role = await checkUserRole();
if (role !== 'parent') {
  document.querySelector('[data-page="admin"]').style.display = 'none';
}
```

---

## 第七部分:重要约束(开发时必须遵守)

### 7.1 业务规则约束

⚠️ **以下规则不能改,改了会破坏与备考手册的一致性**:

1. **积分汇率**: 1 分 = SGD 0.5 (硬编码)
2. **加分项数值**: 不能改(已和家长达成共识)
3. **扣分**: 仅 -10 分一种,不能加其他扣分项
4. **总积分上限**: 第一阶段满分 ~370,正常 ~280
5. **里程碑周**: W4 / W8 / W12 / W14 / W20 / W26(对应备考手册)
6. **不能买游戏类**(包括 Switch / Roblox / PS5 等所有游戏 / 充值)
7. **可扣到负数**,但负数期间不能兑换

### 7.2 UI/UX 约束

1. **角色必须是原创**(避免抄袭锁哥锤等版权角色)
2. **沮丧表情持续 24 小时**,不能更长(避免太严厉打击孩子)
3. **不能因为扣分脱掉装备**(已解锁的装备永久保留)
4. **升级动画必须存在**(主要激励手段)
5. **关键周必须高亮**(W4/W8/W12/W14/W20/W26)

### 7.3 数据约束

1. **状态结构必须包含 version 字段**(为后续数据迁移)
2. **logs 必须包含 timestamp**(用于排序和"4 周滚动"计算)
3. **localStorage key**: `'chamui_psle_v1'`(改了会丢老数据)
4. **数据导出格式**: JSON,文件名 `chamui_psle_YYYY-MM-DD.json`

---

## 第八部分:开发与部署

### 8.1 本地开发

```bash
# 直接打开
open chamui_app_single.html

# 或起本地服务器(支持 file:// 协议下不能用的功能)
python3 -m http.server 8000
# 访问 http://localhost:8000/index.html
```

**开发时拆分文件**:
- 编辑 `index.html` / `character.js` / `data.js` / `app.js`
- 用合并脚本生成 `chamui_app_single.html`(部署用)

**合并脚本** (build.py 或类似):
```python
with open('index.html') as f: html = f.read()
with open('character.js') as f: char = f.read()
with open('data.js') as f: data = f.read()
with open('app.js') as f: app = f.read()

merged = html.replace(
    '<script src="character.js"></script>\n<script src="data.js"></script>\n<script src="app.js"></script>',
    f'<script>\n{char}\n\n{data}\n\n{app}\n</script>'
)
with open('chamui_app_single.html', 'w') as f: f.write(merged)
```

### 8.2 部署选项

#### 选项 A: GitHub Pages(免费,有 URL,数据本地)

1. 注册 GitHub 账号
2. 创建仓库 `chamui-psle`(Public)
3. 上传 `chamui_app_single.html` → 重命名为 `index.html`
4. Settings → Pages → Source 选 main 分支 → Save
5. 等 1-2 分钟 → URL: `https://用户名.github.io/chamui-psle/`

#### 选项 B: Vercel / Netlify(免费,自动部署)

```bash
# 用 Vercel CLI
npm i -g vercel
vercel
# 跟着提示选项目
```

#### 选项 C: Firebase Hosting + Firestore(完整方案)

需要做的:
1. 创建 Firebase 项目
2. `firebase init hosting + firestore`
3. 把代码改成 Firebase 版(参考 6.4)
4. `firebase deploy`

### 8.3 测试指南

**手工测试清单**:
- [ ] 各级升级动画正常
- [ ] 装备解锁条件正确
- [ ] 加分/扣分日志记录无误
- [ ] 数据导出/导入完整
- [ ] 重置后能恢复初始状态
- [ ] 周次切换不丢数据
- [ ] 兑换扣减积分正确
- [ ] 关键周(W4/W8/W12/W14/W20/W26)有特殊项目
- [ ] 沮丧表情 24h 后自动消失
- [ ] 打卡可以取消(再点一次)
- [ ] 历史曲线图随积分变化
- [ ] 移动端响应式正常

**自动化测试**(未实现,可后续添加):
建议用 Playwright 做端到端测试:
```javascript
const { test, expect } = require('@playwright/test');

test('加分流程', async ({ page }) => {
  await page.goto('http://localhost:8000');
  await page.click('[data-page="admin"]');
  await page.click('button:has-text("＋20 分")');
  expect(await page.locator('#totalPoints').innerText()).toBe('20');
});
```

---

## 第九部分:文件清单

### 当前项目所有相关文件

| 文件 | 用途 | 大小 |
|------|------|------|
| `index.html` | UI 骨架 + CSS | 拆分版 |
| `character.js` | 锤锤 SVG 渲染 | 拆分版 |
| `data.js` | 数据 + 规则 | 拆分版 |
| `app.js` | 主应用逻辑 | 拆分版 |
| `chamui_app_single.html` | 单文件部署版 | 63 KB |
| `锤锤的备考冒险.html` | = chamui_app_single.html(中文文件名) | 63 KB |
| `锤锤备考_部署指南.md` | 给家长看的部署教程 | 文本 |
| `PSLE_2027_完整备考总手册_v9.pdf` | 配套备考手册 | 79 页 |
| `PSLE_2027_积分激励系统.pdf` | 积分规则单页 | 2 页 A4 |

---

## 第十部分:开发优先级建议

如果你要继续用 Claude Code 开发,推荐顺序:

### 🔴 P0 - 立即做(影响实际使用)

1. **Firebase 云同步** — 让父母和孩子真正能看到同一份数据
2. **简单的角色权限** — 加个密码/PIN,孩子不能进管理页

### 🟡 P1 - 体验提升(1-2 周做)

3. **PWA 支持** — 能装成桌面应用,体验大升级
4. **Service Worker 离线缓存** — 没网也能打开
5. **每日任务**(不只是周任务)— 让锤锤每天都有事干
6. **音效反馈** — 加分/升级有动听音效

### 🟢 P2 - 功能扩展(慢慢做)

7. **第二阶段周卡片**(W27-W52)— 第一阶段结束后才需要
8. **AI 作文评分集成** — 上传作文图片自动评分
9. **多语言** — 英文版给孩子玩
10. **更多装备 / 成就 / 外观** — 玩到后期不无聊

### ⚫ P3 - 远期(看反馈再决定)

11. **多孩子支持** — 一个家庭多个学生
12. **家长群组** — 跟其他家长比进度
13. **学习数据分析** — 趋势图、薄弱章节预警

---

## 第十一部分:重要提示给 Claude Code

如果你是 Claude Code,正在读这份文档:

### 必读

1. **不要随意改业务规则**(参考第七部分约束)
2. **保持单文件部署的能力**(用户需要 GitHub Pages)
3. **保持数据兼容性**(version 字段,旧数据要能升级)
4. **保持视觉风格**(Q 萌可爱,不要变商务范)

### 推荐做法

1. **先跑起来再改**: 双击 `chamui_app_single.html` 在浏览器打开,看清楚现状
2. **用 Playwright 测试**: 改完用浏览器自动化测试一遍主流程
3. **分文件改,合并部署**: 编辑 4 个拆分文件,build 出单文件
4. **保留向后兼容**: 改了状态结构的话,在 `loadState` 里做迁移

### 沟通建议

如果用户(家长)有新需求,先问清楚:
- 是给**家长**用还是**孩子**用?(决定 UI 复杂度)
- 是**远程同步**还是**本地用**?(决定是否要 Firebase)
- 是想**激励**还是想**约束**?(决定加分还是扣分)
- 是**临时**还是**长期**?(决定是否值得做架构改动)

### 已知问题

1. 沙盒环境字体加载会失败(沙盒无网络),实际使用没问题
2. wkhtmltoimage 渲染 JS 不完整,测试用 Playwright 更准
3. Canvas 图表在窗口大小变化时不会自动重绘(没加 resize 监听)

---

# 📚 v14 - v18 演进史(2026-04 → 2026-05)

> 本节记录从 v0.1 单纯打卡工具 → v18 上瘾级 PSLE 备考游戏 的完整改造。  
> 文件结构、心理学锚点、所有功能、state 字段、关键函数 一文打尽。  
> 接手开发的人只看这一节就能上手继续做。

## 版本时间线

| 版本 | 日期 | 主要内容 |
|---|---|---|
| v0.1 | 2026-04-30 | 单文件打卡(MVP) |
| v14 | 2026-05-01 | 升级到 PSLE 手册 v14, 73 周日表 + 9 时段 + 学科词汇 500 词 |
| v15 | - | 文案打磨 + 装备扩 22 件 |
| v16 | 2026-05-01 | 升级到手册 v16, 加 6 铁律 + 周日复盘 + 听力 modal |
| v17.1 | 2026-05-01 | 🔥 Daily Streak 5 级损失厌恶 + 🤯 73+30 Wow 事实 |
| v17.5 (Phase 2) | - | ❓ 神秘宝箱(70/25/5)+ 🤔 14 反直觉思考题 |
| v17.6 | - | 删今日铁律 + 名师秘诀多科按日轮换 + streak 改 hover tooltip + 黑体 + 字体放大 |
| v17.7 (Phase 3+4) | - | 宝箱 → tab 栏 + 紧凑卡 + ⭐ 每日任务 + 🎮 词汇连连看 mini-game |
| v18 (Phase 5.1+5.3+5.4) | 2026-05-01 | 🐣 宠物 + 🏆 30 成就 + 🎁 每日抽奖 + 🔁 间隔重复 + 🌅 未来自我 + 🔊 音效 + 3 mini-game |
| v18.1 | - | 中文化 streak/combo 文案 |
| v18.2 | 2026-05-01 | 宠物贴近角色 + 数据可视化(思考题/词汇/听力 7 天趋势) |
| v18.3 | 2026-05-02 | mini-game 题库扩(数学 79 P5/P6 + Editing 25 段 + 听写 12 段)+ 按日轮换 |
| v18.4 | 2026-05-02 | 🐹 宠物换小仓鼠主题 + 紧凑布局(master tip 不再拉伸 549px) |

---

## v14-v16: PSLE 手册数据 + 听力/词汇/学习辅助

### 数据结构核心

**WEEK_TASKS**(73 周, 每周 39+ 个 slot,源:PSLE_2027_完整备考总手册_v16.docx)
- 每周 `{ week, date, theme, goal, days: { Mon: { E1, OR, VC, LS, ED, S2, VB }, Tue: ..., Sat: { AM, PM, ... }, Sun } }`
- 9 个 slot keys: AM/PM(周末整段)/ E1(英语主项 1h)/ OR(口试)/ VC(学科词汇)/ LS(听力)/ ED(Editing)/ S2(段 2 主科目)/ VB(Vocab/华文)
- 提取脚本:`C:/Users/Eric/AppData/Local/Temp/chuichui/extract_v16_docx.py`
- 总 2855 个 task slot

**SLOT_TIME**: 每个 slot 的时长描述
**slotPoints(weekN, slotKey)**: 时长加权分数 — AM/PM=3 / E1·S2=2 / 其它=1, 难章周(⭐) +1
**KEY_SLOT_KEYWORDS**: 周复盘必做关键 slot 正则

### 学科词汇(v14)

**VOCAB_500**: { math: { weeks: '1-7', total: 200, sections: [{title, words[]}] }, sci: { weeks: '8-17', total: 300, sections } }
- 数学 200 词分 5 类(几何/数与运算/比与比例/数据/动词)
- 科学 300 词分 9 类(物质三态/力/热/光/电/植物/动物/生态/实验)
- `getVocabForWeek(weekN)` 返回该周对应词表
- **VOCAB_MEANINGS**: 238 个英中翻译(用于词汇连连看 mini-game)

### v16 加的内容

**IRON_RULES**: 6 条铁律(原主页卡, **v17.6 删除**)
**SUNDAY_REVIEW_STEPS**: 周日 19:30 复盘 5 步(coach card 周日才显示)
**WEEK_THEMES[73]**: 每周主题字符串(WEEK_TASKS.theme 同步)
**LISTENING_RESOURCES**: 9 项听力源(CNA938 直播 + 3 PSLE 真题 + 4 频道 playlist)
- 类型: live-audio / youtube / youtube-playlist
- 难度徽章: PSLE 真题 / B2 中高级 / B2-C1 / C1 高级 / 成人级
- 防沉迷: 每天 30 min, 上限到自动锁,家长输密码可解锁

**SGD_PER_POINT = 0.25**: 兑换汇率(1 积分=0.25 SGD, 6000 分=SGD 1500 终极大奖)

---

## v17.1: 🔥 Daily Streak (Kahneman 损失厌恶)

### State

```js
state.dailyStreak = {
  days: 0,             // 当前连击
  lastDate: null,      // 'YYYY-MM-DD' 最近打卡日
  bestEver: 0,         // 历史最高(墓碑)
  freezeTokens: 0,     // 🧊 救命券(每 14 天 +1, 上限 3)
  brokenAt: null       // 上次断点时间戳(24h 灰烬)
}
```

### 关键函数

- `bumpDailyStreak(state)` — 在 `setDailyCheck` 时调,每天首次打卡触发
  - 返回 `{ added, days, isMilestone, gainedFreeze, brokenInfo }`
  - 如断了,自动消耗 freezeToken 救一天 OR 真断
- `streakSeverity(days)` — 0-4 级断点严重度
- `parentRestoreStreak(state)` — 父母强制恢复 streak

### 5 级断点惩罚

| streak | 视觉 | modal | 装备灰 |
|---|---|---|---|
| 1-3 天 | toast | "🔥 火苗灭了" | 无 |
| 4-6 天 | 角灰 | 小 modal "💔 X 天断了" | 无 |
| 7-29 天 | 灰烬 24h | **全屏必点** "💔💔" + 历史最高 | 7 天装备灰 |
| 30-99 天 | streak 装备灰 24h | 全屏 + **3 秒延迟** | 30 天装备灰 |
| 100+ 天 | 角色卡黑白 24h | **5 秒倒计时** + **扣 50 分** | 全部 streak 装备灰 |

### Streak 装备(character.js)

`condition: 'streak-days'`, value 7/30/100 天 → 自动解锁。

---

## v17.2: 🤯 Wow 事实(Loewenstein 信息缺口)

### 数据

- **WEEKLY_WOW_FACTS[73]**: 每周 1 条科学事实绑课程主题, `{ week, hook, body }`
- **ENGLISH_WOW_FACTS[30]**: 独立英语 wow 池(词源/语法/习语/词汇/写作/阅读/听力/Cloze/Editing/Grammar/Oral/Synthesis/Vocab/PSLE 13 类)

### 按日轮换

`getTodayWowFact(weekN, dateOverride)`:
- 周一/三/五/日 = **英语**(epochDay % 30 选)
- 周二/四/六 = **科学**(本周 WEEKLY_WOW_FACTS)
- 同日多次调返回相同(epoch hash 稳定)

### UI

- 主页 `wow-card-big` (右栏 hero-right-column)
- 默认展开 body(v17.3),点击切换收起/展开
- ✨ 角落星星动画
- 每展开一次:`state.wowSeenCount++` + enqueue 间隔重复

---

## v17.5 Phase 2

### ❓ 神秘宝箱(Skinner 变量奖励)

**State:**
```js
state.mysteryBoxes = {
  available: 0, opened: 0,
  totalSlotsAtLastEarn: 0,
  history: []  // 最近 10 次 [{tier, points, wow, equipId, ts}]
}
```

**关键函数:**
- `awardMysteryBoxesIfDue(state)` — 在 toggleDailyCheck 后调,每完成 10 个 slot +1 box
- `openMysteryBoxOnce(state)` — 70/25/5 概率
  - 70% common: +5 分
  - 25% wow: +15 分 + 弹一条英语 wow
  - 5% rare: 解锁未到阈值的高分装备(自动补分)

**v17.7 起:** 宝箱 entry 从主页卡 → tab 栏 5th 按钮(action 不切页),`renderMysteryBoxCard()` 只更 tab 计数。

### 🤔 反直觉思考题(Schultz 预测奖赏)

**THINK_PUZZLES[14]**: 难章周配, `{ week, subject, question, options[4], correct, explanation }`
**State:** `state.thinkPuzzleAnswers = { weekN: { answer, correct, ts } }`

**逻辑:**
- 打卡页顶部 `think-puzzle-card`(只难章周显示)
- **必先选才看答案**(预测奖赏机制)
- 答对 +10 / 答错 +5(都加分,鼓励思考过程)
- enqueue 间隔重复

---

## v17.6: 多科名师秘诀

### 4 个池

- `ENGLISH_MASTER_TIPS[26]` (3 天/周)
- `SCIENCE_MASTER_TIPS[21]` (2 天/周)
- `MATH_MASTER_TIPS[6]` (1 天/周)
- `CHINESE_MASTER_TIPS[6]` (1 天/周)

### 按日轮换

`getTodayMasterTip(weekN, date)`:
- 周一/三/五 = 英语
- 周二/四 = 科学
- 周六 = 数学
- 周日 = 华文
- epochDay % pool.length 选

**UI:** `master-tip-card` 在 hero-right-column,subject 徽章配色。

### 其它 v17.6 改动

- 删除 `iron-rule-card` HTML + render 调用
- 删除 hero-right `streak-rules-card`,改为 streak block 上 `title=` tooltip + `onclick=showStreakRulesModal()`
- 全局 `* { font-family: PingFang SC, Microsoft YaHei, SimHei, ...!important }`
- html 17px / body 16px / card-title 19px / 全局 +2-4px

---

## v17.7 Phase 3 + 4

### ⭐ 每日特别任务(Daily Quest, Goal Gradient)

**DAILY_QUEST_POOL[8]:** 每日上午自动选 1 个 24h 内有效
- wow-1: 看 1 条 wow → +5
- think-1: 答 1 道思考题 → +10
- listen-15: 听 15 min → +10
- slot-3: 打 3 项目 → +5
- slot-5: 打 5 项目挑战 → +12
- box-open: 开 1 宝箱 → +3
- vocab-1: 看 1 个词汇本 → +5
- feynman: 教家人(手动确认)→ +8

**State:** `state.dailyQuests = { 'YYYY-MM-DD': { questId, progress, target, completed, claimedAt } }`

**关键函数:**
- `getTodayQuest(state)` — epoch hash 选,同日稳定
- `bumpQuestProgress(state, questId, amount)` — 自动追踪
- `_trackQuest(questId, amount)` — app.js 内部包装,在 6 处 hook(toggleDailyCheck/wow click/think submit/box open/vocab open/listen tick)

### 🎮 词汇连连看 mini-game(Csikszentmihalyi Flow)

- 6 英文 + 6 中文双列点配(用 VOCAB_500 + VOCAB_MEANINGS)
- 错抖动 0.6s 复位
- 全对 → +10 分 + 1 宝箱 + spawnConfetti
- 5 min/局,算 5 min 听力额度

---

## v18 Phase 5.1 + 5.3 + 5.4(2026-05-01 大版本)

### 🐹 宠物(IKEA Effect, v18.4 改仓鼠主题)

**PET_FORMS[7]:** 仓鼠蛋 → 仓鼠宝宝(3d) → 小仓鼠(7d) → 学习仓鼠(14d) → 智慧仓鼠(30d) → 战神仓鼠(60d) → 仓鼠王者(100d)

**State:**
```js
state.pet = {
  name: '球球',           // v18.4 默认 (老用户保留自定义)
  formIdx: 0,
  feedCount: 0,
  happiness: 100,         // streak 断 -50, 每天打卡 +5
  lastFedDate, spawnedAt
}
```

**关键函数:**
- `getCurrentPetForm(state)` — 按 streak.bestEver 算
- `feedPet(state)` — toggleDailyCheck 时调
- `petBreaksHappiness(state)` — streak 断时调

**UI:** `pet-widget` 在 character-card 内 absolute(top:60, right:8),圆形 56×56 + ::after tooltip 小标签。点击弹 `petModal`(改名 + 进化预览)。

### 🏆 30 隐藏成就(Bartle Achiever)

**ACHIEVEMENTS[30]** — 5 大类(坚持/探索/收藏/宝箱/PSLE)+ 6 个隐藏(夜猫子/早起鸟/周日奉献/马拉松/神龙伙伴/好奇者)

**State:**
```js
state.achievements = {
  unlocked: ['streak_7', ...],
  unlockedAt: { id: ts }
}
```

**关键函数:**
- `checkAchievements(state)` — 返回新解锁数组
- `_checkAndUnlockAch()` — app.js 包装,在 6 处 hook(checkin/think/box/vocab game/admin tab)
- `_showAchievementUnlock(a)` — 全屏 modal + 撒花 + pop 音

**UI:** 主页 `#achievementWallCard` (展开看 grid)+ `#achievementUnlockModal` 弹屏。

### 🎁 每日登录抽奖(Compulsion Loop)

**State:**
```js
state.dailyDraws = {
  fragments: 0,           // 7 片合成 1 完整 box
  consecutive: 0,         // 连续登录天数
  lastDrawDate: null
}
```

**关键函数:**
- `checkDailyDraw(state)` — 同日重复返回 null
- `_checkDailyDrawOnInit()` — init 800ms 后调
- 7 天必中 1 整盒, 14 天 +wow, 30 天 +rare

### 🔁 间隔重复(Ebbinghaus / SuperMemo)

**State:**
```js
state.spacedRepetition = {
  reviews: {
    'wow:eng_5': { firstSeen, lastReviewed, intervalDays: 3, correctStreak: 0 },
    'think:5': {...}
  }
}
```

**关键函数:**
- `enqueueReview(state, type, id)` — wow click / think submit 时调
- `getDueReviews(state)` — 主页 `renderReviewCard` 用
- `submitReview(state, key, correct)` — 答对 ×2 间隔, 错重置 3 天

**UI:** 主页 `#reviewCard`(只有 due 时显示),问题 + ✅记得 / 🤔 忘了 按钮。

### 🌅 未来自我预览(Hyperbolic Discounting 解药)

**关键函数:** `predictFutureSelf(state)` 返回:
```js
{ predictedTotal, predLv, predEqCount, predAL, daysLeft, avgDaily, breakRate }
```

算法:
- avgDaily = totalPoints / (currentWeek × 7)
- breakRate = 0.85 (有 streak) / 0.50 (无)
- predictedTotal = totalPoints + avgDaily × daysLeft × breakRate
- predAL: 6000+ → AL6, 4500+ → AL8, 3500 → AL10, 2500 → AL12, ...

**UI:** 升级进度卡按钮 "🌅 看 W73 的我" → `#futureSelfModal` 显示对比。

### 🔊 音效系统(Multi-modal, Web Audio API 程序合成)

6 种 — ding / fanfare / slot / tada / sad / pop  
**0 KB 体积成本**(纯 OscillatorNode 合成,不引 mp3)

**关键函数:** `playSound(type)`,内部 `_playTone(ctx, freq, dur, wave, vol, delay)`  
触发:打卡 / 升级 / 开盒 / 全对 / 错答 / 成就解锁  
管理页"🔊 音效开关" → `state.soundEnabled = false` 关。

### 🧪 3 个新 mini-game

**➗ 数学速算:**
- v18.3: 79 题 P5/P6 PSLE 真难度,8 类(分数/百分比/速度/比例/平均/面积/小数/GST)
- 30 秒答 10 题, 全对 +8 / ≥7 +5
- `getDailyMathQuestions(10)` 按日洗牌,同日稳定

**✏️ Editing 找错:**
- v18.3: 25 段, 每段 ~50 词 5 错
- 段落点击词,正确变绿,错抖动
- 找到 5/5 → +10 + 1 宝箱
- `getDailyEditingParagraph()` 按日选

**🎧 听写练习:**
- v18.3: 12 段,1 句 ~20 词 5 空
- 用 SpeechSynthesis API 程序播,**不引音频文件**
- 全对 → +10 + 1 宝箱
- `getDailyListenDictation()` 按日选

**入口:** 主页 `mini-game-hub-card` → `openMiniGameHub()` 4 按钮 modal。

---

## v18.2 + v18.4 UI 优化

### 数据可视化(v18.2)

**`renderProgressStatsCard()`** 在分数页(history)顶部:
- 4 个 stat 块:思考题进度 / 词汇游戏胜率 / 成就解锁 / mini-game 总数
- 7 天听力柱状图(`.ls-trend` + `.ls-bar` 7 根渐变柱)
- state 加 4 个计数:`vocabGameRuns / mathGameRuns / editingGameRuns / listenGameRuns`

### 紧凑布局(v18.4)

**根因:** `hero-section { align-items: stretch }` + `master-tip-card { flex: 1 1 auto }` → 名师秘诀被强制拉到 character-card 高度(680px),浪费 549px 空白

**修:**
```css
.hero-section { align-items: start; }   /* 不再 stretch */
.master-tip-card {                       /* 不再 flex 拉伸 */ }
.master-tip-content {                    /* 不再 flex:1 */ }
```

**字体/padding 全局再瘦:**
- card padding 14×16 → **10×14**
- character-svg 150 → **130**
- 各模块 -2-4px

效果:第一屏多 1-2 张卡可见。

---

## 完整 state 字段清单(v18.4)

```js
{
  // 核心
  version, currentWeek, totalPoints,
  daily,                    // { weekN: { dayKey: { slotKey: bool } } }
  weekly,                   // { weekN: { checkin: {} } }
  streakBonusCount, monthlyTestPass,
  milestones,               // { W14, W20, W26, W42, W52, W65, W68, W72, W73 }
  sadUntil, logs, exchanges,
  scores,                   // { 'week_day_slot': { score, max, note, savedAt } }

  // v5+
  activeSkin: 'default',
  adminPassword: null,
  equipmentDisabled: [],

  // v16
  listeningUsage: {},       // { dateKey: { seconds } } 防沉迷

  // v17
  dailyStreak: {},          // 连续打卡
  mysteryBoxes: {},         // 神秘宝箱
  thinkPuzzleAnswers: {},   // 思考题答题
  dailyQuests: {},          // 每日任务

  // v18
  pet: {},                  // 仓鼠
  achievements: {},         // 30 成就
  dailyDraws: {},           // 每日抽奖
  spacedRepetition: {},     // 间隔重复
  soundEnabled: true,       // 音效

  // misc tracking (用于成就 + 数据可视化)
  wowSeenCount, vocabPerfectRuns, vocabGameRuns,
  mathGameRuns, editingGameRuns, listenGameRuns,
  adminPageOpens, marathonStreak, rareBoxesCount,
  _lateNightChecked, _earlyMorningChecked
}
```

---

## 心理学覆盖矩阵(v18.4 完成)

| 机制 | 实现位置 | 评分 |
|---|---|---|
| Loewenstein 信息缺口 | Wow + Think | 8/10 |
| Schultz 预测奖赏 | Think 先猜 + Mystery box | 8/10 |
| Vasilyeva 反直觉震撼 | 14 思考题 | 8/10 |
| Csikszentmihalyi Flow | 4 mini-game | 8/10 |
| Bandura 自我效能 | Feynman + 词汇全对 | 7/10 |
| Skinner 变量奖励 | Mystery box + 抽奖碎片 | 9/10 |
| Kahneman 损失厌恶 | Streak 5 级 + 宠物饿 | 9/10 |
| Ariely Goal Gradient | 每日任务进度条 | 8/10 |
| IKEA Effect | 宠物改名 | 7/10 |
| Bartle Achiever | 30 成就 | 9/10 |
| Cialdini 一致性 | 每日抽奖连登 | 8/10 |
| Ebbinghaus 间隔 | 间隔重复 | 8/10 |
| Gilbert Future Self | W73 预览 | 7/10 |
| Multi-modal | Web Audio 6 种 | 7/10 |

**14 个心理学机制全部到位**, 综合 ~8.5/10(原 v0.1 ~3/10)。

---

## 文件结构(v18.4)

```
chamui-psle/
├── index.html         (~620KB build, ~70KB raw) UI + 全部 CSS + 25+ modal 容器
├── app.js             (~2400 行) 所有交互/渲染/逻辑
├── data.js            (~2700 行) 所有数据 + state shape + helpers
├── character.js       (~700 行) SVG 角色 + 6 皮肤 + 45 装备
├── chamui_app_single.html (574KB) build.py 合并的部署版
├── build.py           (Python 一键合并 4 文件 → single)
├── deploy.sh          (一键 build + commit + push GitHub + Firebase)
├── qa_check.js        (Node 跑, 146 项一致性检查)
├── README.md          家长概览
├── 开发说明文档.md     (本文件)
├── 部署指南.md         GitHub Pages + Firebase 设置
├── SETUP_FIREBASE.md   Firebase 配置流程
└── SETUP_IPAD.md       iPad 17 域名白名单
```

---

## 接手开发 Checklist

读完本节后:

1. **跑起来** — `python -m http.server 8766 --directory C:/claude/chamui-psle` → 打开 chamui_app_single.html
2. **改代码** — 改 4 个拆分文件之一,然后 `python build.py` 重新合并
3. **测试** — `node qa_check.js` 必须全过(146 项)
4. **部署** — `./deploy.sh "commit msg"` 一键 push GitHub + Firebase
5. **验证** — 打开 https://chamui-psle.web.app 看新版

**核心原则(改代码前必读):**

- ✅ **state 加新字段必须在 `getDefaultState()` 加默认值**(老设备 Object.assign 兜底兼容)
- ✅ **新功能要 hook 进 `_checkAndUnlockAch()`** 如果会触发成就
- ✅ **新功能要 hook 进 `_trackQuest(questId, amount)`** 如果对应每日任务
- ✅ **音效用 `playSound(type)`** 不要直接调 Web Audio API
- ✅ **新数据数组要 expose 到 window** 否则其他文件用不了
- ✅ **改 state shape 要更 qa_check.js** 加新断言
- ❌ **不能 saveState 一个空默认 state**(防覆盖 Firestore 历史 — 已有安全闸 isEmptyDefaultState)
- ❌ **不能在 testing 中真的写 saveState**(用 in-memory copy 测,测后还原)

**未做的事(留给下次):**
- ❌ Phase 5.2(故事线 + 6 phase boss)
- ❌ Phase 5.5(个性化 / 反思日记 / 节日 / 家庭模式)
- ❌ 知识地图(skill tree visualization)
- ❌ 真机 iPad 跨设备 v18 全功能验证

详细未完成清单见 plan 文件:`C:\Users\Eric\.claude\plans\md-http-md-readme-md-http-readme-md-abundant-cray.md`

---

**v14-v18 演进史完。继续往下读 Phase 5.2/5.5 时,从 plan 文件入手。**🚀

如果有问题,可以参考备考手册 v9 PDF 来理解每个里程碑的业务含义。
