# chamui-psle 待开发规划（backlog 具体方案）

> 基于 2026-05 暑期会话中讨论过、但尚未落地的开发内容
> 当前线上: v19.39b + 缓存根治 · 分支 `master`
> 部署铁律: `python build.py && node qa_check.js`(必全过) → `git push origin master` → `npx firebase deploy --only hosting`

优先级依据：孩子英文老师 5-29 反馈的真实弱点 —— **Writing B册 OE 4/15 (27%)** + **T3 看图作文**，其余不弱。

---

## P1-A｜Picture Composition 看图作文（T3 重灾区，最高优先）

### 目标
T3 考"看图描述 + 展开 + 表达观点"，现有 73 个 Composition prompt 是 emoji 文本非真图，无专门看图训练。

### 数据（data.js，新增 ~400 行）
```js
const PICTURE_COMPOSITIONS = [
  {
    id: 'pc_2018_friendship', year: 2018, theme: '友谊与责任',
    pictures: [
      { label: 'A 教室', emoji: '🏫📚✏️', desc: '同桌借笔没还' },
      { label: 'B 操场', emoji: '⚽🏃👫', desc: '朋友摔倒你帮他' },
      { label: 'C 家里', emoji: '🏠📺💬', desc: '弟弟撒谎你劝他' }
    ],
    prompt: '看 3 图, 任选 1 图为主线, 写 150+ 词记叙文. 必含: 描述情景 + 你的感受 + 个人观点',
    suggestedVocab: ['guilty','remorse','resolved','integrity','persuaded'],
    structure: 'Opening(设悬念) → Conflict(用对话) → Resolution(情感升华) → Personal opinion',
    sampleOpening: 'The shrill ring of the bell echoed as my hands trembled, clutching the borrowed pen...'
  }
  // ... 10-15 套 PSLE 2015-2024 风格 (原创, 非复制版权真题)
];
```

### 函数（app.js，~120 行）
- `openPictureCompositionGame(id?)` — 三图 grid + prompt + 结构提示 + 建议词 chip + 写作 textarea
- `submitPictureComp(id, text)` — 存 V1/V2/V3 多稿 + 自动数词 + 高级词命中计数（对照 suggestedVocab / 50 高级词）
- `getPictureCompositionByDate(date)` — 按 SUMMER_CURRICULUM 派题

### UI
- Modal 顶部 3 图横排卡（`.picture-comp-grid`，手机竖排），emoji 大字 + 场景描述
- 中部 prompt + 结构四步 + 5 个建议词
- 写作区: textarea 实时字数 + 高级词高亮 + 命中数
- 底部 V1/V2/V3 tab + 自评 rubric

### 接入
SUMMER_CURRICULUM 里 W2 周一/三/五 C 时段 + W4 周三 X2 的 `fn` 改成 `openPictureCompositionGame`。

### 工作量
用户出题源 ~2h（10-15 套三图格 emoji 化）+ 开发 ~3h。

---

## P1-B｜OE Booklet B 真题模式（对应 4/15 弱点）

### 目标
现有 `openCompOeGame` 是阅读理解 OE（读 passage 答题），非 Booklet B 看图/图文 OE。加专门的 B册模式 + 分数追踪 4/15 → 进度。

### 数据（data.js，~500 行）
```js
const OE_BOOKLET_B_PAPERS = [
  {
    id: 'oeB_2020_p1', year: 2020, paper: 'Booklet B P1',
    sections: [{
      type: 'visual-text', picture: { emoji: '🏞️🚣', desc: '湖边一家四口+小船' },
      passage: 'On a windy Saturday morning, the Tan family decided to go for a picnic...',
      questions: [
        { q: 'Why did Mr Tan choose Saturday?', marks: 2, sampleAnswer: '...', rubric: ['windy','family time'] },
        { q: 'How did the children feel? Give 2 reasons.', marks: 3, sampleAnswer: '...', rubric: ['feeling word','2 reasons'] }
        // 5-8 题/passage, 合计 15 分/套
      ]
    }]
  }
  // ... 10 套
];
```

### 函数（app.js，~150 行）
- `openOeBookletBGame(id?)` — passage + 图 + 逐题答题区
- `submitOeAnswer(qid, text)` — 学生答 + 自评 1-3 分滑块（可选家长上传批改）
- `_scoreOeAnswerByRubric(answer, rubric)` — 关键词匹配估分
- 答完显示 sample answer 对比 + rubric 命中 + 总分 X/15 → `state.oeBookletBHistory`

### 接入
- W4 周一 6-22 + 周五 6-26 的 X3 chip `fn` 改成 `openOeBookletBGame`（基线 + 终测）
- 主页 Paper 2 突击卡旁加"Booklet B 突击"入口
- 追踪 4/15 → 目标 10/15+

### 工作量
用户出题源 ~2h + 开发 ~4h。

---

## P2｜词汇闪卡重做（上次做了又回退，需一次定清）

### 背景
v19.40-v19.47 反复迭代（例句/英文解释/考题/放大/网格）最后全回退。现词汇闪卡 = 原始（正面英文→翻面中文，逐张艾宾浩斯）。`VOCAB_MEANINGS`（440 中文）还在，`VOCAB_EN`/`VOCAB_SENT`/`VOCAB_QUIZ` 已删。

### 重做前必须先定清 3 件事（避免再返工）
1. **反面显示什么** — 建议最终版: 中文 + 📖英文解释(短语) + 💬例句 + 📝考题（4 段）
2. **交互** — 逐张翻卡 vs 一页网格点击。用户上次倾向**网格**（点 deck → 所有单词卡网格 → 点卡看详情 modal）
3. **卡片尺寸** — 不要过大（460×420 适中），文字 ≥14px

### 数据（data.js）
- `VOCAB_EN[word]` — 短语英文解释（kid-friendly, 3-10 词）
- `VOCAB_SENT[word]` — P5 级例句
- `VOCAB_QUIZ[word]` — 每词一道 PSLE 风格考题（Cloze 四选一 / 作文升级 / 科学 MCQ / 数学计算）

### ⚠️ 硬要求：科学/数学考题答案逐条核准（防误导孩子）
易错点（上次踩过）:
- photosynthesis 不能写化学配平式（P5 超纲）→ "阳光+水+CO2→食物+氧气"
- germination "不需光"（PSLE 陷阱）
- evaporation 任何温度都发生（非只沸腾）
- decomposer = 真菌/细菌（非昆虫）
- conduction 固体 / convection 液气 / radiation 射线
- prime 最小质数 2（1 不是）; area cm² / perimeter cm 单位; 商=算式结果自洽
- 生成后用独立 agent adversarial 复核 + QA 断言强制校验

### 工作量
生成 440 词 3 字段 ~2 agent 并行 + 复核 + 注入 ~3h。**建议一次性完成不要分批改**。

---

## P3｜其他 backlog（低优先）

### 💎 水晶进化中心 UI
- `state.craftCrystals` 已有数据（成就解锁给💎），装备进化按钮埋在装备墙每张卡里，缺集中入口
- 加 `openCrystalCenter()` modal: 列所有可进化装备 + 够/不够💎 分色 + 一键进化
- 头部💎 pill + 主页大字卡子行可点击跳入
- 新增💎收入渠道（知识树 3⭐/diff6 满分/错题 10 倍）+ 解锁 toast

### 错题本扩展
- 现仅部分 mini-game 入库，扩展到 vocab/listen/editing/scilab 剩余

### 其他（更早提过）
- 名人堂截图分享（拿金龙后角色+日期+总分入名人堂）
- 金龙幼崽完整养成（现仅显示，无独立喂养/进化）
- 季节事件（PSLE 100 天倒计时装备）

---

## 建议实施顺序

| 阶段 | 内容 | 触发时机 |
|---|---|---|
| **1** | P1-A Picture Composition + P1-B OE Booklet B | 孩子跑完 W1 (6-1~6-7 OE 攻坚)后, 看错题分布再启动 |
| **2** | P2 词汇闪卡重做 | 先一次性定清 3 件事再动手 |
| **3** | P3 💎水晶/错题本/其他 | 前两阶段稳定后 |

## 通用注意事项（上次踩坑）
- **改完关键文件用 grep/git 验证真落盘**，别信工具 success；部署后 curl 实锤线上内容
- shell 中文/emoji 输出会花屏 → 关键结果写文件再读或 `grep -c` 数字化
- 分支是 **master 非 main**；缓存已根治，改动普通刷新即生效
- 题库/常量/UI 改动后 `qa_check.js` 必全过（缺一即报错）
